@model ContactMessageReadViewModel
<li class="nav-item dropdown hidden-caret">
    <a class="nav-link dropdown-toggle" href="#" id="messageDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fa fa-envelope"></i>
        <span class="notification">@Model.contactMessages.Count</span>
    </a>
    <ul class="dropdown-menu messages-notif-box animated fadeIn" aria-labelledby="messageDropdown">
        <li>
            <div class="dropdown-title d-flex justify-content-between align-items-center">
                Messages
                <a href="#" class="small">Mark all as read</a>
            </div>
        </li>
        <li>
            @foreach (var item in Model.contactMessages)
            {
                <div class="message-notif-scroll scrollbar-outer">
                    <div class="notif-center">
                        <a href="#">
                            <div class="notif-img">
                                @if(item.ProfileImage is null)
                                {
                                    <img src="~/images/user/uknown.jpg" alt="Img Profile">
                                }else
                                {
                                    <img src="~/images/user/@item.ProfileImage" alt="Img Profile">
                                }
                               
                            </div>
                            <div class="notif-content">
                                <span class="subject">@item.Name</span>
                                <span class="block">
                                    @item.Message
                                </span>
                            </div>
                        </a>
                    </div>
                </div>
            }       
        </li>
        <li>
            <a class="see-all" href="javascript:void(0);">See all messages<i class="fa fa-angle-right"></i> </a>
        </li>
    </ul>
</li>
